# -*- sh -*-

eval "$(rbfu --init --auto)"

alias cscurl="curl -s -E $HOME/.ssh/bbc/certificate.pem -H 'Accept: application/json' -H 'X-Candy-Platform: desktop' -H 'X-Candy-Audience: international'"

# Without cat, get python stack track if you abort pipe
alias jsonlint="python -mjson.tool | cat"

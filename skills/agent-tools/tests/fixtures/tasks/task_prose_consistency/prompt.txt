Harmonize the terminology across all chapters in `book/` to use "Quantum Core" consistently as specified in `book/glossary.md` (eliminate occurrences of "Q-Core" and "Quantum Engine"). Also fix broken relative Markdown links between chapters so they point to existing files (e.g. `chapter2.md` and `chapter3.md`).

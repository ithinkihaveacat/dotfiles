Synchronize `docs/client_guide.md` with the current implementation in `src/client.py`.
Update parameter names (`dataset_id`, `timeout_seconds`, `retry_limit`, `enable_compression`), default values (30, 3, True), explanations, and the Python code snippet in the guide to match `src/client.py` exactly.

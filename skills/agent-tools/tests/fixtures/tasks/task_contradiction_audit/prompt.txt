Audit all policy documents in the `policies/` directory to detect internal factual contradictions.
Generate a structured Markdown report in `audit_report.md` detailing:
1. Every pair of conflicting policies.
2. The specific conflicting statements/numbers (e.g. log retention periods, equipment stipends, background check timelines).
3. The relevant file paths.
4. Proposed reconciliation recommendations.

{
  "name": "@dotfiles/mcp-shell",
  "version": "1.0.0",
  "description": "MCP server for executing shell commands",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-shell": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.25.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}

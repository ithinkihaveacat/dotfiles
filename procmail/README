To add procmail filtering to a dreamhost account, create a file called
$HOME/.forward.postfix with the contents:

"|/usr/bin/procmail"

(Including the quotes!)

Then, symlink the procmailrc in this directory to $HOME/.procmailrc.

(For some reason the .forward.postfix can't be a symlink.)

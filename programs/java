# -*- sh -*- 

CLASSPATH="$HOME/local/lib/java"

for jar in $HOME/local/lib/java/*.jar ; do
  if [ ! -e $jar ]; then
    break
  fi
  CLASSPATH="$CLASSPATH:$jar"
done

# CLASSPATH=".:$CLASSPATH::"

export CLASSPATH

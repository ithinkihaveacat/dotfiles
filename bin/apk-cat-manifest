#!/usr/bin/env bash

require() { hash "$@" || exit 127; }

require apkanalyzer
require xmllint

if [ -z "$1" ]; then
  echo "usage: $(basename "$0") apk"
  exit 1
fi

apkanalyzer manifest print "$1" | xmllint --format -

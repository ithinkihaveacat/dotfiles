#!/usr/bin/env bash

CONFIG=".prettierrc"

if [ -f $CONFIG ]; then
  echo "error: $CONFIG already exists, aborting"
  exit 1
fi

cat << CONFIG_END > $CONFIG
{
  "tabWidth": 2,
  "useTabs": false,
  "trailingComma": "none",
  "proseWrap": "always"
}
CONFIG_END

#!/usr/bin/env bash

usage() {
  cat <<EOF
Usage: $(basename "$0") PACKAGE URL

Opens a URL within a given package.

This script is a convenient wrapper around 'adb shell am start' to launch a
URL within a specific application. This is useful for testing deep links.

Arguments:
  PACKAGE     Android package name (e.g., com.example.app)
  URL         The URL to open

Options:
  -h, --help  Display this help message and exit

Examples:
  # Open a website in Chrome
  $(basename "$0") com.android.chrome https://www.google.com

  # Open a deep link in a specific app
  $(basename "$0") com.example.app myapp://deeplink
EOF
  exit 0
}

if [[ "$1" == "-h" || "$1" == "--help" ]]; then
  usage
fi

require() { hash "$@" || exit 127; }

require adb

if [ $# -ne 2 ] ; then
  usage
fi

adb exec-out am start -W -a android.intent.action.VIEW -d "$2" "$1"

#!/usr/bin/env bash

require() { hash "$@" || exit 127; }

require scrcpy

filename="$1"

if [ -z "$filename" ]; then
  filename=$(date -u +'adb-screenrecord-%Y%m%d%H%M%S.mp4')
fi

scrcpy -N -t -w -r "$filename"

#!/usr/bin/env bash

usage() {
  cat <<EOF
Usage: $(basename "$0")

Deprecated. Displays a message suggesting to use 'git init' instead.

Options:
  --help      Display this help message and exit
EOF
  exit 0
}

if [[ "$1" == "--help" ]]; then
  usage
fi

echo "deprecated: use 'git init' instead"

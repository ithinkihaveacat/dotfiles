#!/usr/bin/env bash

# May also need to adjust paths in fish/config.fish if bumping versions here

nodejs-install "$(nodejs-install -l | tail -1)" "$NODE_VERSIONS"

nodejs-install "$(nodejs-install -l | grep v14 | tail -1)" "$NODE_VERSIONS"

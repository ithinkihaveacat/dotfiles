#!/usr/bin/env bash

require() { hash "$@" || exit 127; }

require ffmpeg

filename="$1"

if [ -z "$filename" ]; then
  echo "usage: $(basename "$0") filename"
  exit 1
fi

ffmpeg -i "$1" -c copy -an "$1"

#!/usr/bin/env bash

if [ $# == 0 ]; then
  echo "usage: $(basename "$0") bugreport.zip"
  exit 1
fi

unzip -p "$1" 'bugreport*.txt' | perl -ne 'print if /^SERVICE com.google.android.gms\/.nearby.exposurenotification.service.ExposureNotificationInternalService/ .. /^$/'

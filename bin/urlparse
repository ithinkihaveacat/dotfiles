#!/usr/bin/env php
<?php

if ($argc == 1) {
    echo "usage: $argv[0] url\n";
    exit(1);
}

$parts = parse_url($argv[1]);

if ($parts && $parts["query"]) {
    parse_str($parts["query"], $parts["query"]);
}

echo json_encode($parts, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES), "\n";

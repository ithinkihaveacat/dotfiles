#!/usr/bin/env bash

require() { hash "$@" || exit 127; }

require adb

if [ -z "$1" ]; then
  echo "usage: $(basename "$0") packageName"
  exit 1
fi

adb shell cmd package compile -r bg-dexopt "$1"

#adb shell cmd "$1" bg-dexopt-job

#!/usr/bin/env bash

VERSION="7.2.0"

test ! -e ".envrc" && touch .envrc

grep "use node" .envrc || echo "use node $VERSION" >> .envrc
grep "layout node" .envrc || echo "layout node" >> .envrc

direnv allow

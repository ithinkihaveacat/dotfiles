#!/usr/bin/env bash

# Returns the Window Id of the frontmost/raised/active window, in decimal.
# (To facilitate comparison with $WINDOWID.)

printf "%d" $(xprop -root _NET_ACTIVE_WINDOW | awk '{ print $NF }')

# https://github.com/sharkdp/bat#configuration-file

# Remove grid borders, line numbers, and header decorations
--style="plain"

# Always use colors (even when piped)
--color="always"

# Use the terminal's 16 ANSI colors
--theme="ansi"
